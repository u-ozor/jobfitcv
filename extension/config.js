// extension/config.js — single source of truth for the API base URL.
// Change this ONE line if you run the backend on a different port
// (must match the PORT value in your .env / start.sh).
const BASE_URL = "http://localhost:8000";
